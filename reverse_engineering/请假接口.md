# 智慧江财「学生请假」接口勘察(2026-09-09 抓包闭环,主要完成)

## 应用定位
- 「学生请假」= 平台 combineApp 短链入口(appid **1617076082728**,applicationId 292,`shortUrl=studentLeaveApply`);真实业务=**金智流程引擎**:
  - 发起申请 app:appid **1617072114687**,`/jxufe/flowEngine/processManage/processApply/shell.w?flowId=120&defId=applicationForLeave:1:1298232&nodeId=startevent_1`
  - 我的申请(草稿/参与):appid **1590716335264**,`/jxufe/flowEngine/processManage/processParticipate/index_draft.w?id=120&code=01&type=1&sign=1&jumpTitle=我的学生请假申请`
  - 学生销假:appid 1619594870676 `shortUrl=studentLeaveBackApply`(流程同构)
- 认证(全平台统一):平台 GUID → checkAppAuth(appid) → result 含 pageUrl(**JWT**:`jwtToken=Bearer eyJ...` sub=GUID,iat=exp 差 900s=15 分钟短效)、`accessKey=GUID`、`userId=<enc base64>`、username=enc;URL query 传递 jwtToken/accessKey/userId/jssdk_url/appid。GET pageUrl → 302 → 版本化页面(v-xxxx-zh_CN-5/)。
- 版本前缀会变:processApply 现 `v-v6jiEz-zh_CN-5`,shell.w 302 后取页面内 $model/UI2 模块 + 同目录 .w.js/.js 已编译可直接 GET(如 .../processParticipate/index_draft.js)。
- 请求头:UA 浏览器非 python + Referer `https://servicewechat.com/wx70c0beda0bb7b021/143/page-frame.html`;均 POST(JSON?看 getApplicationForLeaveInfo 等)或带 jwtToken query。

## 数据接口(全部实测 200)
| 接口(POST base=https://wxcourse.jxufe.edu.cn/interface/api/interface/) | 用途 |
|---|---|
| getAccessMobileApp | shortUrl→访问点清单(发起/我的申请 app_url 等) |
| getFlowUserInfo / verifyInitiationPermission | 流程启动权限/用户 |
| publicInterfaceFlow | 流程定义(flowDef formId 122, caption 学生请假申请流程) |
| getCurrentActHandProcessFlow | 我的申请列表(flowListGroup[code 01 日常管理 id120]+ page query jsonp 模式:pageNo/pageSize/statusFlag/wxUsername=学号/code=01/type=1/id=120/forwardUrl=/management/workFlow/myParticipantFlows/list.htm) |
| **getApplicationForLeaveInfo** | 请假记录明细表(result.result[] 本账号:leave_type/leave_start/leave_end/leave_days/leave_reason/leave_status_name/apply_username/class_hours/leave_nanchang/building_dormitory/parent_phone/my_phone/leave_file/instanceId/id/create_datetime…) |
| getProcessDetailsInfoFlow | 流程实例详情(instanceInfo:status_flag 100 已结束/cur_task_name 流程已结束/start_time/end_time/form_keys=事由摘要) |
| getHiProcessCommentsFlow | 审批历史(hiList: act_name/oper_time/意见) |
| queryNodeFormName | 节点→动态表单:form_path applicationForLeave、formId(申请页 174/结束查看 199)、parameter_name getApplicationForLeaveInfo|
| universal_form_4/api/newVersion/formInfo/getForm&getRespData | 动态表单 schema+回填(表单 id 174 申请【学生请假申请】/199 查看;字段 leave_type[事假/病假/课假…]、leave_start/end、leave_days、leave_reason、class_hours(课假节次)、leave_file(病假附件)、leave_nanchang、parent_phone、my_phone 等) |
| baas/.../EncryptUtil/username | enc 用户转换 |

发起流程的真实「提交/保存草稿」接口未捕获(用户仅到提交页)。表单页含「保存草稿」按钮(不启动流程,安全),补抓草稿保存可拿到提交参数。

## 发起提交链路(2026-09-09 前端 js 逆向补齐,无需再抓包)
**结论:无「保存草稿」接口(表单无草稿按钮),提交=表单数据 + publicInterfaceFlow(submitInstance.htm)**。
- 表单 UI 链:processApply/shell.w → shell.w.js require UI2 processApply/shell(壳)→ windowContainer 加载 **add.w**(同目录;已 GET:add.w.html 34KB/add.w.js 7KB/add.js 37KB)→ add.js 内 iframe1 加载 universal_form_4 form.html?params={formId:174,jumpFormPageFlag:flowIframe}(form.html 156KB = 表单引擎,layui,#submit 按钮在其内)→ form.on('submit(go)') 组装 dataList([{fid:174,formControlId:<控件id>,moduleKey:<attrName>,content:<值>}...]) → **flowIframe 模式不走 OA submit**,置 verifyStatus=true + resultParentOAData=dataList 交还父页(add.w)。
- add.js finishedBtnClick(=「提交申请」按钮 finishedBtn onClick):childVerifyFormAndGetReturnData() 取回 dataList → 组 **formParams**(基础:wxUsername 学号/navTabId""/routeCheck"on"/actorLookup.nextActorUsername+nextActorCnname+nextActorGroup(下一步处理人,来自 userData 选人)/flowId 96/operUserId 平台 openUserId/curOperUserName/wxUsername/nextOutNodeId+nextTaskNodeId(路由转向,取 nextRouteList)→ 学生请假首次发起路由固定流程定义:flowId 71→nextRoute 的 sf.id/fn.id 与 nodeId)/curTaskNodeId:parent.nodeId(startevent_1)/instanceId""/recordId""/jwtToken parent.jwtToken/accessKey/accessKey GUID/appid 1617072114687/callback callback/flag weixin/**forwardUrl /management/workFlow/instance/submitInstance.htm**/jsonpCallback/defId applicationForLeave:1:1298232/accept accept/ + 每个 moduleKey→content 平铺) → action=**publicInterfaceFlow** params={dataMap:JSON.stringify(formParams), jwtToken, accessKey, appid}(POST https://wxcourse.jxufe.edu.cn/interface/api/interface/publicInterfaceFlow;微信态=requestInterface 自动注入顶层 username=GUID+bn_timestamp/bn_nonce/bn_sign,签名算法=校历同款 bainian!@#)→ 成功 data.result.statusCode=="200" → 跳 DRAFTLISTHREF(我的申请)。
- 另一分支(action=URLS.submitFlowInstance,dataMap=v.data,同 forwardUrl 语义)用于非 flowIframe 的 jsPath 表单(请假不走此分支)。
- config.js(URLS 枚举,flowEngine/style/js/config.js):含 **submitFlowInstance**(processApply add)。LOCALHOST=/usr/local/apache-tomcat-8.5.23/webapps/flowEngineNeedDealt/v-v6jiEz-zh_CN-4;DOMAIN http://flow.jxufe.edu.cn。
- 发起页初始化流(进表单即发,微信实测抓包):verifyInitiationPermission → publicInterfaceFlow(dataMap applyNewInstance.htm: defId/nodeId/flowId 71/wxUsername)→ getFlowUserInfo(个人信息)。
- 取到的前端资源已存 %TEMP%\_qj_web\{form.html 148KB,shell.w,shell.w.js,u2_shell.js(shell.js 1.8KB),add_add_js.txt(add.js 37KB),add_add_w_html.txt,u2_config.js,u2_wxCommon-1.0.0.js,draftList.js,index_draft.js,handleList.js,index_handle.js};schema 大文件 %TEMP%\_qj_174_schema.txt(577KB:getForm 174 + getRespData 174/199)。App 实现可全部从这些文件核对。
- **App 提交需真实验证一次(会建真实待审批假条):填合理事由提交一单即证明全链;App 端实现时 dataMap 组装照抄 add.js(formParams 字段全集见上);「处理人」若流程自动,可先取 applyNewInstance 路由首个 nextActor 填充。**

## App 设计结论(待实现)
- feature `lib/features/leave/`:JWT 每次 checkAppAuth 现取(15min 短效)→ 「我的请假」列表(调 getApplicationForLeaveInfo)+详情(表单 199 数据/审批流)。发起请假:表单字段 schema(174)+ 提交 publicInterfaceFlow(submitInstance.htm,formParams dataMap 照抄 add.js)。发起走真实提交验证方式待与用户确认。


## 应用定位(2026-09-09,GUID 直连实测)
- 智慧江财平台应用清单(`/platForm/api/littleProgram/application/*`)中:**「学生请假」= combineApp H5**
  - appid: **1617076082728**、applicationId: **292**、classify 1(教学)/publicFlag 0
  - url: `/jxufe/combineApp/index.w?shortUrl=studentLeaveApply`(热门应用 count=3878)
- 访问链(与校历/网费同款,平台 GUID 即账号标识):
  1. `GET https://wxcourse.jxufe.edu.cn/platForm/api/littleProgram/application/checkAppAuth?appid=1617076082728&platformUsername=<GUID>` → code 200
     result: name=学生请假, type=3/H5, username=`AAAAAAAAAAAAAAAAAAAAAA==`(平台 enc userId,与网费同串), **pageUrl 完整可用**:
     `https://wxcourse.jxufe.edu.cn/1617076082728//jxufe/combineApp/index.w?shortUrl=studentLeaveApply&cardinfo=KLod…&sno=0252513&openId=W0yHUmN7O4fP5/0qA7AFMPSj+DaZC5/ZF0wcbGKRjmc=&deptName=计算机与人工智能学院…&deptCode=143&cname=某同学什&sex=男`
     (cardinfo/openId 每次 checkAppAuth 重新签发,短时效;cname/sno/deptCode 等即当前账号信息)
  2. GET pageUrl → WeX5 v3 页面(`<script src ../../system/...>`,context.model=`$model/UI2/jxufe/combineApp/index.w`),校历同框架 → 页面内动态 require 业务模块并调数据接口。
- 请求头(平台双层过滤经验):必须 UA=浏览器(非 python-requests)+ Referer `https://servicewechat.com/wx70c0beda0bb7b021/143/page-frame.html`;UTF-8 JSON。
- 应用清单接口全表(主包 app-service.js 实证,base=`https://wxcourse.jxufe.edu.cn/platForm/api/littleProgram/application/`):
  checkAppAuth / getAppDetail / getPublicAppForVisitor(游客,GBK 尾响应但 body UTF-8) / getHotAppsByPlatformUsername?platformUsername=GUID(返回全量热门,含请假) / getNewAppsByPlatformUsername / getAllAppByUsername / getMyAppsByUserDefaultRole / getAppClassify / insertUserAccessAppRecord 等。

## 数据接口(待抓包)
请假列表/假别/申请提交接口未知(WeX5 运行时加载,静态难枚举)。
下一步:mitmproxy 抓 PC 微信打开「学生请假」操作一次 → 从 flows 提取业务 js 与接口 URL(目标模式可能是
`https://wxcourse.jxufe.edu.cn/.../studentLeaveApply/...` 的 POST JSON)。

## App 侧设计(待实现,pattern 参考 electricity/net_fee/school_calendar)
- feature 建议 `lib/features/leave/`:请假列表(状态/时间/假别)+ 申请(假别/起止/事由)视接口而定。

## App「记录查询」实现(2026-09-10,用户拍板:先只做记录列表+详情只读,发起后续单做)
- **GUID 直连实测成功(无需 jwtToken/accessKey)**:POST 到 `https://wxcourse.jxufe.edu.cn/interface/api/interface/*`,请求体=业务参数+自动注入 username=平台 GUID+bn_timestamp/bn_nonce/bn_sign(复用校历 `bnSignedBody`,appid 按调用方,见 wxcal_remote_datasource.dart)。response 外层 `{code:1,result:{statusCode:200,...}}`,详情接口为三重嵌套 `result.result.result[]`。
- 三个调用方(全部 appid **1590716335264**「我的申请」,defId applicationForLeave:1:1298232):
  1. **列表** getCurrentActHandProcessFlow(我参与的流程):body{callback,flag weixin,jsonpCallback,forwardUrl /management/workFlow/myParticipantFlows/list.htm,wxUsername 学号,pageNo,pageSize,formKeys"",startTime"",endTime"",statusFlag"",type 1,id 120,code 01} → result.page.result[];行字段 status_flag(0 流转中/100 已结束)/def_description 学生请假申请流程/sequence_name 当前节点/startor_realname/form_keys 事由(带「姓名-」前缀)/cur_task_name/instance_id/id/start_time/end_time(ms)/startor_dept_name/startor_group_name。
  2. **详情** getApplicationForLeaveInfo{defId,instanceId,wxUsername,pageNum,pageSize} → result.result.result[0] 单条(leave_type/leave_reason/leave_start/leave_end/leave_days/leave_status_name/apply_cnname/apply_username/class_name/apply_dept_name/building_dormitory/leave_nanchang 是否离昌/class_hours 课假节次/my_phone/parent_phone/create_date 年月日/id/instanceId)。
  3. **审批历史** getHiProcessCommentsFlow{forwardUrl /management/workFlow/pageModule/dispActHistInfo.htm,defId,wxUsername,instanceId} → result.hiList[](oper_time ms/act_name/sequence_name/comment 意见/assign_cnname/assign_dept_name);学生请假流=学生申请→班主任→学院分团委→学院党委副书记。
- 实现文件(lib/features/leave/,analyze 0 告警):
  - domain/leave_models.dart:LeaveListRecord{instanceId,statusFlag,inProgress,defDescription,sequenceName,formTitle(_stripNamePrefix 去「姓名-」),startedByName/Dept/Group,startedAt/endedAt(ms)}/LeaveDetailInfo(全字段)/LeaveApprovalStep/LeaveDetailBundle。
  - data/datasources/leave_remote_datasource.dart:LeaveApiException;base https://wxcourse.jxufe.edu.cn(注意 .edu.cn 非 .cn);fetchList/fetchDetail/fetchApprovals 三方法组 bnSignedBody(guid,appid 1590716335264,params)。
  - data/providers/leave_providers.dart:leaveDioProvider(.edu.cn+Referer servicewechat+UA)/leaveRemoteDataSourceProvider/leaveGuidProvider=wxGuidProvider(复用校历,共享 box 'wxPlatform' key 'guid')/leaveListProvider/leaveDetailBundleProvider family(instanceId;审批失败不阻断详情)。
  - presentation/leave_screen.dart:记录列表(状态 chip 审批中/已结束,icon schedule/task_alt,行 onTap→详情),GUID 缺失引导+「启用实时源」配置对话框(与网费同款,存 wxPlatform box)。
  - presentation/leave_detail_screen.dart:信息卡(类型·天数/状态/请假时间/是否离昌/课假节次/宿舍/申请人/班级/学院/电话)+事由卡+审批时间线(圆点+竖线,处理人/意见/时间)。
  - 入口:home_screen.dart 宫格在网费后加「请假」(Icons.event_note_outlined);feature_palette.dart 加 leave=Color(0xFF6A1B9A)(_tileColors 同步插入 netFee 后)。
- 演示:配置 GUID 后进「请假」→ 显示真实 7 条记录(2 条审批中),点任意行看详情+审批流(班主任包晗秋等)。
